#version 450

//layout(location = 0) out float depth;

//out float depth;
//out vec4 color;

void main(void)
{
    //depth = gl_FragCoord.z;
    //depth = gl_FragCoord.z;
    //color = vec4(vec3(gl_FragCoord.z), 1.0f);

}
